import { ShellPage } from '@/components/layout/ShellPage';

export default function DashboardPage() {
  return (
    <ShellPage
      title="Dashboard"
      description="Trip and expense overview — implemented in later slices."
    />
  );
}
